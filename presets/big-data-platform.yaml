# Big-data pipeline: per-node collection, partition-coupled processing.
# Storage (object store, warehouse) belongs in dependencies:, not here.
umbrellaChartName: big-data-platform
subcharts:
- name: data-ingestion
  pattern: node-agent
- name: data-processing
  pattern: stream-processor

# IoT platform: device API, per-node collectors, async analytics.
# The MQTT broker and time-series DB belong in dependencies:, not here.
umbrellaChartName: iot-platform
subcharts:
- name: device-manager
  pattern: api-microservice
- name: data-collector
  pattern: node-agent
- name: analytics-engine
  pattern: worker

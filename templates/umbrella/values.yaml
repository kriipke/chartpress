# NOTE: this file is a placeholder for template development only.
# The chartpress engine replaces it at generation time with a values.yaml
# derived from the generation spec: a `global:` block plus one stanza per
# subchart (see internal/engine/values.go).

global:
  # Registry/organization prefix applied to every subchart image.
  repository: registry.example.com/CHANGEME

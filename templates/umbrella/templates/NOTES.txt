Release {{ .Release.Name }} ({{ .Chart.Name }}-{{ .Chart.Version }}) deployed to namespace {{ .Release.Namespace }}.

Check workload status:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

This chart was generated by chartpress as a starter — finish it before relying
on it in production. Search the chart for "TODO(chartpress)" markers:

  1. Set global.repository and each subchart's image name/tag.
  2. Size resources requests/limits per subchart.
  3. Point liveness/readiness probes at real endpoints (or switch them to
     tcpSocket/exec for non-HTTP services).
  4. Set ingress.host (and tls) on the subcharts you expose.
  5. Review podCount per subchart: static replicas vs dynamic (HPA).
  6. Add config/secrets values, or wire existing ConfigMaps/Secrets via
     env/envFrom.
  7. Enable pdb once a subchart runs 2+ replicas; review networkPolicy.

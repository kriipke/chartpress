apiVersion: v2
name: umbrella-chart
description: A Helm chart for Kubernetes
type: application

# Chart version; bump on every change to templates or values.
version: 0.1.0

# Version of the application(s) this umbrella deploys. Subcharts default their
# image tag to their own appVersion, so keep those accurate instead.
appVersion: "0.1.0"

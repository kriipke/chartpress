apiVersion: v2
name: component
description: A Helm chart for Kubernetes
type: application

# Chart version; bump on every change to templates or values.
version: 0.1.0

# Version of the application this chart deploys. The image tag defaults to
# this value, so keep it accurate (or pin image.tag in values.yaml).
appVersion: "0.1.0"

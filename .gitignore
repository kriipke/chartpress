/web/node_modules
output/*
!output/.gitkeep
.claude
skills-lock.json

# Compiled Go binaries — build from source (Dockerfile / `go build`), never commit.
/chartpress
/chartpress-server

# Web build output — regenerated by `npm run build`; never commit.
/web/dist

# Local production values — may contain real secrets; never commit
/chart/values-prod.yaml

# Per-env DO overlay written by `make wire-do` from terraform outputs.
# Derived + environment-specific (bucket/region/endpoint) — regenerate, don't commit.
/chart/values-do.generated.yaml

# macOS
.DS_Store
